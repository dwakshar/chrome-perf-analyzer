{
  "name": "chrome-perf-analyzer",
  "version": "1.0.0",
  "description": "Chrome DevTools extension for capturing Core Web Vitals and surfacing performance issues.",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "sync-version": "node ./scripts/sync-version.mjs",
    "build": "npm run sync-version && webpack --config webpack.config.ts --mode development",
    "watch": "webpack --config webpack.config.ts --mode development --watch",
    "type-check": "tsc --noEmit",
    "build:prod": "npm run sync-version && webpack --config webpack.config.ts --mode production",
    "package": "npm run build:prod && powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/package-release.ps1",
    "validate": "npm run type-check && npm run build:prod"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "@types/node": "^20.19.37",
    "@types/webpack": "^5.28.0",
    "copy-webpack-plugin": "^12.0.2",
    "ts-loader": "^9.5.4",
    "ts-node": "^10.9.0",
    "typescript": "^5.9.3",
    "webpack": "^5.105.4",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "buffer": "^6.0.3"
  }
}
